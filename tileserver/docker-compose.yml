services:
  tileserver:
    image: mirror-docker.runflare.com/maptiler/tileserver-gl:latest
    container_name: tileserver
    ports:
      - "9090:8080"
    volumes:
      - .:/data
    restart: unless-stopped
