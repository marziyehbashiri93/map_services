# PostGIS — source for custom layers
DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=viuna_map
DB_USER=postgres
DB_PASS=your_password
SCHEMA=public

# Set FORCE=1 to rebuild every layer, ignoring the incremental cache
FORCE=
